[pytest]
# test_edge_cases.py is a legacy interactive smoke script that requires cloud API
# keys at import time; it is not part of the automated provider-independent suite.
norecursedirs = .git
addopts = --ignore=test_edge_cases.py
